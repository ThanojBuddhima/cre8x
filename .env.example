# CARTO basemap key for the live tracking map.
# Without it the map still works, using CARTO anonymous tiles.
# This value ships in the client bundle, so restrict it by domain in CARTO.
VITE_CARTO_KEY=
